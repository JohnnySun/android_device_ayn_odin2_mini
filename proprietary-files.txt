# Odin2 Mini stock blob extraction list.
#
# Keep this file narrow at first. Add entries only after inventory confirms the
# stock path and the consuming HAL/package in the target tree.

# Stock Odin apps used for behavior study. Do not ship these once the
# replacement stack covers the feature.
system/app/OdinSettings/OdinSettings.apk
system/app/TouchMapping/TouchMapping.apk
system/app/GameAssistant/GameAssistant.apk

# Stock Qualcomm device manifest. The stock image stores the Kalama-specific
# source name; the target build consumes the canonical VINTF destination.
vendor/etc/vintf/manifest_kalama.xml:vendor/etc/vintf/manifest.xml

# Add further device-specific firmware/HAL blobs only after the corresponding
# private stock inventory and target consumer are reviewed.
